% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_unaligned_regions.R
\name{get_unaligned_regions}
\alias{get_unaligned_regions}
\title{Combine Unaligned Regions by Chromosome}
\usage{
get_unaligned_regions(
  CHROM,
  ref_genome = "hg38",
  folder,
  selected_clades = NULL,
  threads = 1L
)
}
\arguments{
\item{CHROM}{Character. The chromosome name (e.g., \code{"chr1"}).}

\item{ref_genome}{Character. Reference genome name used for labeling (default \code{"hg38"}).}

\item{selected_clades}{Character vector. Optional subset of species or clades to include.}

\item{threads}{Integer. Number of threads to use for parallel file loading (default \code{1L}).}

\item{input_folder}{Character. Folder containing a folder named \code{"unaligned"} that has per-species unaligned \code{.rds} files.}

\item{output_folder}{Character. Folder to write the combined output. Defaults to \code{input_folder}.}
}
\value{
Saves a combined \code{.rds} file in a subfolder \verb{unaligned_by_chr/} under \code{output_folder}.
}
\description{
This function aggregates unaligned (missing/gap) regions across species for a specific chromosome.
It assumes RDS files (created by \code{get_unaligned_regions()}) are stored in a folder with filenames following
a naming pattern like \code{chr1_species_unaligned.rds}. The function supports multi-threaded reading
and outputs a single \code{GRanges} object with chromosome labels.
}
\examples{
\dontrun{
combined_unaligned_by_chrom("chr1", input_folder = "out/", threads = 4)
}
}
